[project]
name = "python-education-projects"
version = "0.1.0"
description = "Add your description here"
requires-python = ">=3.10"
dependencies = [
    "black>=25.1.0",
    "mypy>=1.15.0",
]

[tool.mypy]
strict = true
